#ifndef DEMO_ADDRESSES_HPP
#define DEMO_ADDRESSES_HPP
#include <logical_address.h>

const LogicalAddress la_LSM(1, 0);
const LogicalAddress la_TempSensor(1, 1);
const LogicalAddress la_CB(1, 2);

#endif
