#ifndef DEMO_ADDRESSES_HPP
#define DEMO_ADDRESSES_HPP
#include <logical_address.h>

const LogicalAddress la_LSM(1, 0);
const LogicalAddress la_temp(1, 1);
const LogicalAddress la_light(1, 2);
const LogicalAddress la_medianFilter(1, 3);
const LogicalAddress la_solarArray(1, 4);

#endif
